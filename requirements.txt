# Clean Dependencies for World-Class Screenshot OCR
# Minimal, production-ready setup

# Core OCR engine (includes PyTorch dependencies)
easyocr>=1.7.0

# Essential image processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Data validation
pydantic>=2.5.0

# Cog prediction framework dependencies handled by base image
